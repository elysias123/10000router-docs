# 微调（Fine-tuning）

NewAPI 将以下 Fine-tuning 接口标记为“未实现”。当前文档只记录兼容路径，不表示网关已经提供训练能力。

| 方法 | 路径 | 状态 |
| --- | --- | --- |
| `GET` | `/v1/fine-tunes` | 未实现 |
| `POST` | `/v1/fine-tunes` | 未实现 |
| `GET` | `/v1/fine-tunes/{fine_tune_id}` | 未实现 |
| `POST` | `/v1/fine-tunes/{fine_tune_id}/cancel` | 未实现 |
| `GET` | `/v1/fine-tunes/{fine_tune_id}/events` | 未实现 |
