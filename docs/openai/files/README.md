# 文件（Files）

NewAPI 将 Files 接口标记为“未实现”。如果网关尚未支持，请保留此状态并返回明确错误，不要把它当作可用上传接口。

## 端点清单

| 方法 | 路径 | 状态 |
| --- | --- | --- |
| `GET` | `/v1/files` | 未实现 |
| `POST` | `/v1/files` | 未实现 |
| `GET` | `/v1/files/{file_id}` | 未实现 |
| `DELETE` | `/v1/files/{file_id}` | 未实现 |
| `GET` | `/v1/files/{file_id}/content` | 未实现 |
